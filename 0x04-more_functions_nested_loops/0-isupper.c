#include "holberton.h"

/**
 * _isupper - check the code for Holberton School students.
 *@c: this is the check
 *
 * Return: 1 if the number is positive. 0 otherwise
 */
int _isupper(int c)
{

	if (c >= 'a' && c <= 'z')
	{
		return (0);
	}
	else
	{
		return (1);
	}}
