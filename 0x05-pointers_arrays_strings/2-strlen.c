#include "holberton.h"

/**
 * _strlen -  that returns the length of a string.
 * @s: value of char
 *
 *Return: Always 0.
 */
int _strlen(char *s)
{
    char abc [60];
    int a;
    a = _strlen(abc);

}
