#include "holberton.h"

/**
 * rev_string - function that reverses a string.
 * 
 * 
 */
void rev_string(char *s)
{
    int i, l;
    char = aux;
	
    l = 0;
	while (s[l] != '\0')
 	{
		l++;
	}
    for (l--; i = 0; i < l / 2; i++, l--)
    {
     aux = s[b];
     s[b] = s[a];
     s[a] = aux;
    }
}
